# ЗАДАЧА №1

Случайный процесс определяется формулой $X(t) = X \cdot cos{ωt}$, где $X$ - случайная величина. Найти основные характеристики этого процесса, если $M(X) = a$, $D(X)=σ^2$.

1.   Математическое ожидание
$M(X \cdot cos𝛚t) = cos{wt} \cdot M(X) = a \cdot cos{𝜔t}$
2.   Дисперсия
$D(X \cdot cos𝛚t) = cos^2{wt} \cdot D(X) = σ^2 \cdot cos^2{𝜔t}$
3.   Корреляционная функция
$K_X(t_1,t_2) = M[(X \cdot cos{wt_1} - a \cdot cos{wt_1}) \cdot (X \cdot cos{wt_2} - a \cdot cos{wt_2})] = cos{wt_1} \cdot cos{wt_2} \cdot M[(X-a) \cdot (X-a)] = cos{wt_1} \cdot cos{wt_2} \cdot D(X) = σ^2 \cdot cos{wt_1} \cdot cos{wt_2}$
4.   Нормированная корреляционная функция
$ρ_X(t_1, t_2) = \frac{cos{wt_1} \cdot cos{wt_2} \cdot D(X)}{σ \cdot cos{wt_1} \cdot σ \cdot cos{wt_2}} = 1$

# РЕШЕНИЕ

```
import numpy as np
import matplotlib.pyplot as plt

om = 1
t = np.linspace(0, 10, 1000)
X = np.random.rand(1000)
x = X * np.cos(om * t)
a = 0.5
xa = a * np.cos(om * t)
d = 1/12
dx = d * np.cos(om * t)**2

plt.figure(figsize=(12, 6)) # Увеличим размер фигуры для лучшей читаемости

# Первый график
plt.subplot(1, 2, 1) # Создаем подграфик 1 из 2
plt.plot(t, x, label='x(t)')
plt.plot(t, xa, label='xa(t)')
plt.plot(t, dx, label='dx(t)')
plt.xlabel('Время, t') # Подпись оси x
plt.ylabel('Амплитуда') # Подпись оси y
plt.title('Зависимость амплитуды от времени') # Заголовок графика
plt.legend() # Добавляем легенду

# Второй график (гистограмма)
plt.subplot(1, 2, 2) # Создаем подграфик 2 из 2
plt.hist(X, bins='sturges') # bins - количество столбцов в гистограмме
plt.xlabel('Случайная величина X') # Подпись оси x
plt.ylabel('Частота') # Подпись оси y
plt.title('Гистограмма случайных величин X') # Заголовок графика


plt.tight_layout() # Автоматически настраивает расположение подграфиков, чтобы избежать перекрытия
plt.show()
```

<img width="1189" height="590" alt="image" src="https://github.com/user-attachments/assets/ba52d2e7-e9af-466f-a5bb-8e09470c4cb1" />
